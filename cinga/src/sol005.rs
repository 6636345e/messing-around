fn main() {
        println!("starting here ...");
}

// each letter contains i.e. 2 = {a,b,s} & 3 = {d,e,f}
// step 1.: Map each number with its letter values
// step 2: Use the user input numbers to mapp their values in values of two

pub fn letter_combinations(digits: String) -> Vec<String> {
        let mut Output = Vec::new();
        let two = ["a", "b", "c"];
        let three = ["d,", "e", "f"];
        let four = ["g", "h", "i"];
        let five = ["j", "k", "i"];
}
